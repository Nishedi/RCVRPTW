
\section{Problem Formulation}

This section presents a mathematical model of the Capacitated Vehicle Routing Problem with Time Windows (CVRPTW) with soft time windows. The model incorporates vehicle capacity constraints, service times, and penalty-based handling of time window violations.


\subsection{Problem Definition}

We assume the existence of $N$ service locations in set $\mathcal{N} = \{1, 2, \dots, N\}$. This set is extended by location 0, representing the depot, creating a set $\mathcal{D} = \{0\} \cup \mathcal{N}$. The depot is the start and end point for each service vehicle. Each location has defined coordinates, based on which the travel cost between locations is calculated. The cost is given in the distance matrix $d_{i,j}$ denoting the travel cost from location $i$ to $j$, $i,j \in \mathcal{D}$.

A fleet of vehicles $\mathcal{V}$ is available, where each vehicle $v \in \mathcal{V}$ has a capacity constraint $Q=90$. Each customer location $i \in \mathcal{N}$ has a demand $y_i$, a service time $s_i$, and a time window $[a_i, b_i], \quad a_i, b_i \in \mathbb{N},\; b_i > a_i
$ during which service should ideally occur.  
The notation used throughout this section is summarized in Table~\ref{tab:notation}.

\begin{table}[ht]
	\renewcommand{\arraystretch}{1.3}
	\caption{Notation summary}
	\label{tab:notation}
	\centering
	\begin{tabular}{l|l}
		\hline\hline
		Symbol & Meaning  \\
		\hline \hline
		$\mathcal{V}$ & Set of vehicles \\ 
        \hline
        $\mathcal{D}$ & Set of all locations (including depot) \\
        \hline
        $\mathcal{N}$ & Set of $N$ customer locations ($\mathcal{D}\setminus\{0\}$) \\
        \hline
        $d_{i,j}$ & Travel cost from location $i$ to $j$ \\
        \hline
        $s_i$ & Service time at location $i$ \\
		\hline
        $[a_i,b_i]$ & Time window for location $i$\\
        % \hline
        % $q_i$ & Demand at customer location $i$\\
        \hline
        $Q$ & Capacity of vehicle $v$ \\
        \hline
        $x_{v,i,j}$ & Binary variable: 1 if vehicle $v$ uses arc $i\rightarrow j$ \\
        \hline
        $z_{v,i}$ & Binary variable: 1 if vehicle $v$ serves location $i$\\
        \hline
        $t_{v,i}$ & Arrival time of vehicle $v$ at location $i$\\
        \hline
        $w_{v,i}$ & Waiting time of vehicle $v$ at location $i$\\
        \hline
        $p_{v,i}$ & Early arrival penalty for vehicle $v$ at $i$ \\ 
        \hline
        $q_{v,i}$ & Late arrival penalty for vehicle $v$ at $i$ \\ 
        \hline
        $y_{i}$ & Load weight servicing location $i$\\
        \hline
        $\alpha$ & Penalty coefficient for time window violations\\
        \hline
        $M$ & Large constant (big-M method)\\
        \hline \hline
	\end{tabular}
\end{table}

\subsection{Mathematical Model}

The mathematical model consists of an objective function that minimizes the total operational cost, subject to a set of constraints that ensure feasibility. The objective function comprises three main components: travel costs, waiting time costs, and penalty costs for time window violations.

\subsubsection{Objective Function}

The complete objective function minimizes the total cost:

\begin{equation}\label{eq:objective}
\begin{aligned}
    \min \; & \sum_{v \in \mathcal{V}} \left( 
         \sum_{i \in \mathcal{D}} \sum_{j \in \mathcal{D}}  x_{v,i,j} d_{i,j} 
        +  \sum_{i \in \mathcal{N}} w_{v,i} \right. \\
    & \left. 
        + \sum_{i \in \mathcal{N}} \alpha (p_{v,i} + q_{v,i}) 
    \right)
\end{aligned}
\end{equation}


where the first term represents the total travel distance, the second term accounts for waiting time costs (vehicle and driver idle time), and the third term penalizes time window violations with penalty coefficient $\alpha$.

\subsubsection{Constraints}

The objective function \ref{eq:objective} is minimized subject to the following constraints:

\textbf{Route structure constraints:}

\begin{equation}{\label{eq:one_visit}}
     \sum_{v \in \mathcal{V}}\sum_{j \in \mathcal{D}\setminus \{i\}} x_{v,i,j}=1, \quad \forall i \in \mathcal{N}
\end{equation}

\begin{equation}{\label{eq:flow_balance}}
    \sum_{j\in\mathcal{D}\setminus\{i\}}x_{v,i,j}-\sum_{j\in\mathcal{D}\setminus\{i\}}x_{v,j,i}=0, \quad \forall v \in \mathcal{V}, i \in \mathcal{N}
\end{equation}

\begin{equation}{\label{eq:edgevisitcorrelation}}
    z_{v,i} = \sum_{j\in \mathcal{D}\setminus\{i\}}x_{v,j,i}, \quad \forall v \in \mathcal{V}, i \in \mathcal{N}
\end{equation}


% \begin{equation}{\label{eq:start_end_in_depot}}
%     \sum_{j\in\mathcal{N}}x_{v,0,j}=z_{v,j}, \quad \sum_{i\in\mathcal{N}}x_{v,i,0}=z_{v,j}, \quad \forall v \in \mathcal{V}
% \end{equation}

\begin{equation}\label{eq:start_depot}
\sum_{j \in \mathcal{N}} x_{v,0,j} = \sum_{i \in \mathcal{N}} z_{v,i}, \quad \forall v \in \mathcal{V}
\end{equation}

\begin{equation}\label{eq:end_depot}
\sum_{i \in \mathcal{N}} x_{v,i,0} = \sum_{i \in \mathcal{N}} z_{v,i}, \quad \forall v \in \mathcal{V}
\end{equation}


Constraint \ref{eq:one_visit} ensures that each customer location is visited exactly once by one vehicle. Constraint \ref{eq:flow_balance} enforces flow conservation: if a vehicle arrives at a location, it must also depart from it. Constraints \eqref{eq:start_depot} and \eqref{eq:end_depot} ensure that each vehicle performing service starts and ends its route at the depot.
Constraint \ref{eq:edgevisitcorrelation} links the visit indicator variable with the routing variables.

\textbf{Time window constraints:}

\scriptsize
\begin{equation}{\label{eq:time_cohestion}}
 t_{v,j} \;\ge\; t_{v,i} + s_i + d_{i,j} \;-\; M(1 - x_{v,i,j}), \quad \forall v \in \mathcal{V}, \forall i,j \in \mathcal{D}, i\neq j
\end{equation}

\begin{equation}\label{eq:start_soft}
p_{v,i} \geq a_i - t_{v,i}-M(1-z_{v,i}), \quad \forall v \in \mathcal{V}, i \in \mathcal{N}
\end{equation}

\begin{equation}\label{eq:penalty}
t_{v,i} \;\le\; b_i + q_{v,i}, \quad \forall v \in \mathcal{V}, i \in \mathcal{N}
\end{equation}

\begin{equation}\label{eq:waiting_time}
w_{v,i} \;=\; t_{v,j}-s_i-d_{i,j}-t_{v,i}-M(1-z_{v,i}), \quad w_{v,i}\ge 0, \forall v \in \mathcal{V}, i \in \mathcal{N}
\end{equation}
\normalsize

Constraint \eqref{eq:time_cohestion} ensures temporal consistency: arrival at the next location cannot occur before completing service at the current location plus travel time. 
Constraints \eqref{eq:start_soft} and \eqref{eq:penalty} implement soft time windows by introducing penalty variables $p_{v,i}$ (early arrival) and $q_{v,i}$ (late arrival). 
Constraint \eqref{eq:waiting_time} defines the waiting time as the difference between the service start time and the vehicle arrival time.



\textbf{Capacity constraints:}



\begin{equation}\label{eq:capacity_flow}
    \sum_{i \in N}z_{v,i}y_i \leq Q
\end{equation}
Constraint \eqref{eq:capacity_flow} ensures that the total demand assigned to a vehicle does not exceed its capacity. 
It limits the sum of customer demands served by vehicle $v$ to the maximum allowable capacity $Q$, guaranteeing feasible load allocation along the route.

\textbf{Variable domains:}

\begin{equation}
     x_{v,i,j}, z_{v,i}\in\{0,1\}, \quad \forall v \in \mathcal{V}, i,j \in \mathcal{D}
\end{equation}

\begin{equation}
     p_{v,i}, q_{v,i}, w_{v,i}, t_{v,i}, \geq 0, \quad \forall v \in \mathcal{V}, i \in \mathcal{D}
\end{equation}

This formulation provides a complete MILP model for the CVRPTW, solvable using commercial solvers or metaheuristic approaches for large-scale instances.
