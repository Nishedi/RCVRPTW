%Parameter Tuning
\subsection{Parameter Tuning}

This section describes the parameter tuning procedure for the Tabu Search algorithm. The goal of parameter tuning is to identify the best combination of algorithm parameters that minimizes the objective function value across diverse problem instances.

\subsubsection{Experimental Design}

The parameter tuning experiments were conducted on six representative Solomon benchmark instances: C101, C201 (clustered), R101, R201 (random), and RC101, RC201 (mixed). For each configuration, three independent runs were performed with different random seeds to ensure statistical robustness.

The following parameters were varied:
\begin{itemize}
    \item \textbf{Mutation Type}: swap, insert, invert -- three different neighborhood operators;
    \item \textbf{Tabu List Size}: 10, 20, 30, 40, 50 -- defining the memory length of the tabu search;
    \item \textbf{Number of Iterations}: 50, 100, 150, 200, 250, 300 -- controlling the search duration.
\end{itemize}

This resulted in $3 \times 5 \times 6 = 90$ unique parameter configurations, each tested on all six instances with three repetitions, yielding a total of 1620 experimental runs.

\subsubsection{Results by Mutation Type}

Figure~\ref{fig:mutation_comparison} presents the comparison of mean objective values achieved by different mutation operators. The \texttt{swap} operator consistently outperforms both \texttt{insert} and \texttt{invert} operators across all tested configurations.

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            bar width=0.8cm,
            width=0.9\columnwidth,
            height=6cm,
            ylabel={Mean Objective Value},
            symbolic x coords={swap, insert, invert},
            xtick=data,
            ymin=10000,
            ymax=13000,
            nodes near coords,
            nodes near coords align={vertical},
            every node near coord/.append style={font=\scriptsize, rotate=0},
            ymajorgrids=true,
            grid style=dashed,
        ]
        \addplot[fill=blue!60] coordinates {
            (swap, 11449.25)
            (insert, 12183.67)
            (invert, 12618.64)
        };
        \end{axis}
    \end{tikzpicture}
    \caption{Comparison of mean objective values by mutation type. Lower values indicate better performance. The \texttt{swap} operator achieves the lowest average objective value.}
    \label{fig:mutation_comparison}
\end{figure}

Table~\ref{tab:mutation_summary} provides detailed statistics for each mutation type. The \texttt{swap} operator not only achieves the lowest mean objective (11449.25) but also demonstrates the smallest range between minimum and maximum values, indicating more consistent performance.

\begin{table}[ht]
    \renewcommand{\arraystretch}{1.3}
    \caption{Summary statistics of objective values by mutation type.}
    \label{tab:mutation_summary}
    \centering
    \begin{tabular}{l|c|c|c|c}
        \hline\hline
        Mutation & Mean & Min & Max & Improvement \\
        \hline\hline
        swap   & 11449.25 & 4981.76 & 25969.94 & 16.98\% \\ \hline
        insert & 12183.67 & 5415.28 & 27506.67 & 11.66\% \\ \hline
        invert & 12618.64 & 5077.71 & 29236.06 &  8.50\% \\
        \hline\hline
    \end{tabular}
\end{table}

\subsubsection{Impact of Tabu List Size}

Figure~\ref{fig:tabu_size_impact} shows the relationship between tabu list size and the mean objective value. The results indicate that larger tabu list sizes (40--50) tend to produce slightly better results, although the differences are relatively small (less than 0.5\%).

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            width=0.9\columnwidth,
            height=5.5cm,
            xlabel={Tabu List Size},
            ylabel={Mean Objective Value},
            xmin=5,
            xmax=55,
            ymin=12050,
            ymax=12110,
            xtick={10,20,30,40,50},
            grid=both,
            grid style={line width=.1pt, draw=gray!30},
            major grid style={line width=.2pt,draw=gray!50},
            mark size=2pt,
            legend pos=north east,
        ]
        \addplot[color=blue, mark=square*, thick] coordinates {
            (10, 12093.79)
            (20, 12095.20)
            (30, 12094.68)
            (40, 12066.91)
            (50, 12068.69)
        };
        \end{axis}
    \end{tikzpicture}
    \caption{Impact of tabu list size on mean objective value. Larger tabu sizes (40--50) show marginally better performance.}
    \label{fig:tabu_size_impact}
\end{figure}

\subsubsection{Impact of Number of Iterations}

Figure~\ref{fig:iterations_impact} illustrates the effect of iteration count on solution quality. The results demonstrate that increasing the number of iterations from 50 to 100--200 significantly improves solution quality, while further increases beyond 200 iterations show diminishing returns.

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            width=0.9\columnwidth,
            height=5.5cm,
            xlabel={Number of Iterations},
            ylabel={Mean Objective Value},
            xmin=25,
            xmax=325,
            ymin=12040,
            ymax=12200,
            xtick={50,100,150,200,250,300},
            grid=both,
            grid style={line width=.1pt, draw=gray!30},
            major grid style={line width=.2pt,draw=gray!50},
            mark size=2pt,
        ]
        \addplot[color=red, mark=triangle*, thick] coordinates {
            (50, 12186.06)
            (100, 12065.22)
            (150, 12064.68)
            (200, 12054.67)
            (250, 12061.42)
            (300, 12071.06)
        };
        \end{axis}
    \end{tikzpicture}
    \caption{Impact of iteration count on mean objective value. Performance improves significantly from 50 to 100--200 iterations, with diminishing returns beyond that point.}
    \label{fig:iterations_impact}
\end{figure}

\subsubsection{Heatmap Analysis: Swap Operator}

Figure~\ref{fig:heatmap_swap} presents a heatmap visualization of mean objective values for different combinations of tabu size and iteration count when using the \texttt{swap} operator. The best configurations cluster around 100--200 iterations with tabu sizes of 40--50.

\begin{figure}[ht]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            colormap/hot,
            colorbar,
            colorbar style={
                ylabel={Mean Objective},
                ytick={11300,11400,11500,11600,11700,11800}
            },
            view={0}{90},
            width=0.95\columnwidth,
            height=7cm,
            xlabel={Number of Iterations},
            ylabel={Tabu List Size},
            xtick={50,100,150,200,250,300},
            ytick={10,20,30,40,50},
            point meta min=11260,
            point meta max=11790,
        ]
        \addplot3[
            surf,
            shader=flat,
            mesh/rows=5,
        ] coordinates {
            (50,10,11785.96) (100,10,11534.31) (150,10,11561.82) (200,10,11530.48) (250,10,11580.04) (300,10,11595.72)
            (50,20,11688.90) (100,20,11423.90) (150,20,11418.80) (200,20,11391.01) (250,20,11429.41) (300,20,11461.91)
            (50,30,11675.87) (100,30,11360.25) (150,30,11394.27) (200,30,11383.27) (250,30,11362.93) (300,30,11453.06)
            (50,40,11546.18) (100,40,11327.75) (150,40,11310.76) (200,40,11307.34) (250,40,11395.91) (300,40,11371.39)
            (50,50,11597.90) (100,50,11263.33) (150,50,11294.76) (200,50,11284.42) (250,50,11365.73) (300,50,11380.02)
        };
        \end{axis}
    \end{tikzpicture}
    \caption{Heatmap of mean objective values for the \texttt{swap} operator across different combinations of tabu size and iteration count. Darker colors indicate lower (better) objective values.}
    \label{fig:heatmap_swap}
\end{figure}

\subsubsection{Best Configuration}

Table~\ref{tab:best_configs} summarizes the best parameter configurations identified for each mutation type. The overall best configuration uses the \texttt{swap} operator with tabu size of 50 and 100 iterations, achieving a mean objective value of 11263.33.

\begin{table}[ht]
    \renewcommand{\arraystretch}{1.3}
    \caption{Best parameter configurations for each mutation type.}
    \label{tab:best_configs}
    \centering
    \begin{tabular}{l|c|c|c}
        \hline\hline
        Mutation & Tabu Size & Iterations & Mean Objective \\
        \hline\hline
        swap   & 50 & 100 & \textbf{11263.33} \\ \hline
        insert & 40 & 150 & 12138.55 \\ \hline
        invert & 10 & 250 & 12407.64 \\
        \hline\hline
    \end{tabular}
\end{table}

\subsubsection{Conclusions from Parameter Tuning}

The parameter tuning experiments lead to the following conclusions:

\begin{enumerate}
    \item \textbf{Mutation operator}: The \texttt{swap} operator significantly outperforms \texttt{insert} and \texttt{invert} operators, achieving approximately 6--10\% lower objective values on average.
    
    \item \textbf{Tabu list size}: Larger tabu sizes (40--50) provide marginally better performance by preventing the search from cycling back to recently visited solutions too quickly.
    
    \item \textbf{Iteration count}: The optimal number of iterations lies in the range of 100--200. Using only 50 iterations results in premature termination, while exceeding 200 iterations shows diminishing returns without significant quality improvement.
    
    \item \textbf{Recommended configuration}: Based on the experimental results, the recommended configuration for subsequent experiments is: \texttt{swap} mutation, tabu size of 30, and 100 iterations. Although the best performing configuration uses tabu size of 50, the performance difference is marginal (less than 0.5\%), and a smaller tabu size provides better computational efficiency while maintaining near-optimal solution quality.
\end{enumerate}

These findings guided the parameter selection for the main experimental evaluation described in the following subsections.
